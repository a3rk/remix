# Configuration for epochd
# Syntax: any command line option may be specified as 'clioptionname=value'.
# See 'epochd --help' for all available options.

data-dir=/var/lib/epoch
log-file=/var/log/epoch/epoch.log
log-level=0
